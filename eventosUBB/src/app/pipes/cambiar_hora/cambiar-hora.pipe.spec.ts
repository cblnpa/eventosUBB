import { CambiarHoraPipe } from '../cambiar_hora/cambiar-hora.pipe';

describe('CambiarHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
