import { ActividadParalelaPipe } from './actividad-paralela.pipe';

describe('ActividadParalelaPipe', () => {
  it('create an instance', () => {
    const pipe = new ActividadParalelaPipe();
    expect(pipe).toBeTruthy();
  });
});
